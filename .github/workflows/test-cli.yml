name: Test CLI

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "crates/databasectl/**"
      - "Cargo.lock"
      - "scripts/classify-install-integration.py"
      - "scripts/tests/test_classify_install_integration.py"
      - ".github/workflows/test-install.yml"
      - ".github/workflows/test-cli.yml"

permissions:
  contents: read


jobs:
  test:
    name: Test dctl
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable @ 2026-03-27

      - name: Build
        run: cargo build -p databasectl

      - name: Clippy
        run: cargo clippy -p databasectl -- -D warnings

      - name: Clippy without default features
        run: cargo clippy -p databasectl --all-targets --no-default-features -- -D warnings

      - name: Test
        run: cargo test -p databasectl

      - name: Verify install workflow path mapping
        run: python3 scripts/tests/test_classify_install_integration.py
